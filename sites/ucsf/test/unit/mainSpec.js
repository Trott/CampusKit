describe('UCSF controllers', function () {

    describe('BackButtonController', function (){

        it('should set hideBackButton to true if path is /', function () {
            var scope = {};
            var location = {
                path: '/'
            };
            //var ctrl = new BackButtonController(scope, location);

            //expect(scope.hideBackButton).toBe(true);
        });
    });
});